# RAGFlow server root and tenant-scoped API key.
RAGFLOW_URL=http://localhost:9380
RAGFLOW_API_KEY=ragflow-your-key

# Optional: credential used only when configuring an external model provider.
RAGFLOW_PROVIDER_API_KEY=
